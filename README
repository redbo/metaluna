This is a python library that lets you create userland virtual block devices by
hijacking the NBD kernel module.  It's mostly for playing around.

The nbd.h header file was blatantly stolen from the nbd-server codebase.
